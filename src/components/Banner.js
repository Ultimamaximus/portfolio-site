import React from 'react';
import styles from '../css/Banner.module.css';

const Banner = () => {
    return (
        <div className={styles.banner}>
            <h2 className={styles.text}>Let's Get In Touch!</h2>
        </div>
    );
};

export default Banner;
